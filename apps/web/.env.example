# Weights & Biases Weave Tracing
# Set your W&B API key for authentication
WANDB_API_KEY=your-wandb-api-key

# Set your Weave project name (format: "team/project" or just "project")
# Use one of these options:
WEAVE_PROJECT=your-team/your-project
# WANDB_PROJECT=your-team/your-project
# CORE_WEAVE=your-team/your-project

# Redis Cloud Configuration
# Used for archiving DAP outputs and intake questionnaire data
# All values are secrets - do not commit actual values to git
REDIS_HOST=your-redis-host.cloud.redislabs.com
REDIS_PORT=12345
REDIS_PASSWORD=your-redis-password-here

# Optional: Connection timeout (milliseconds, default: 5000)
# REDIS_CONNECT_TIMEOUT=5000

# Optional: Disable Redis persistence (for testing, default: true)
# REDIS_ENABLED=true

# Feature Flags
# Enable operations dashboard (default: enabled in development, disabled in production)
# ENABLE_OPS_PAGE=true
